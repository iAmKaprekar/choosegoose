const controller = {};

const db = require('../models/piggyModel');

controller.isComplete = async() => {
  try {
    return next();
  } catch (err) {
    return next({

    })
  }
}

controller.addEntry = async() => {
  try {
    return next();
  } catch (err) {
    return next({

    })
  }
}

module.exports = controller;